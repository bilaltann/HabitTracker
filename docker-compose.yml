version: '3.8'

services:
  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=HabitAdmin2026!
    ports:
      - "1433:1433"
    volumes:
      - mssql-data:/var/opt/mssql

  backend:
    build: 
      context: .
      dockerfile: backend.Dockerfile
    ports:
      - "8080:8080"
    environment:
      # Bu satır, senin appsettings.json'daki yerel DESKTOP bağlantını AWS'de ezer.
      - ConnectionStrings__DefaultConnection=Server=db;Database=HabitTrackerDb;User Id=sa;Password=HabitAdmin2026!;TrustServerCertificate=True;
    depends_on:
      - db

  frontend:
    build:
      context: .
      dockerfile: frontend.Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mssql-data: